import { describe, it, expect } from 'vitest';

/**
 * Dummy test file for composables library
 * This ensures the test runner doesn't fail when no tests are found
 */
describe('composables library', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
